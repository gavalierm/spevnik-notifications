// src/operations/worker.js — placeholder. Implementation in subsequent tasks.
export default {
  id: 'spevnik-notifications-worker',
  handler: async () => {
    // Operation handler body — populated in later tasks.
  },
};
